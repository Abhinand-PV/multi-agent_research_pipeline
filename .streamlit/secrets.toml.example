# Copy these values into Streamlit Cloud:
# App settings -> Secrets -> paste as TOML
#
# For local development, copy to .streamlit/secrets.toml
# (that file is gitignored) or use a .env file in the project root.

GROQ_API_KEY = "your_groq_api_key_here"
OPENAI_AGENTS_DISABLE_TRACING = "1"
